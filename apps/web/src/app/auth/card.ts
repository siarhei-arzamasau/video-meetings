/** `.card` ships 4 units of padding, which suits a dense list card and looks cramped as the
 *  only thing on screen. Utilities win the cascade over it. Shared so the sign-in and sign-up
 *  cards cannot end up different sizes. */
export const CARD_CLASS = 'w-full gap-6 p-8';
